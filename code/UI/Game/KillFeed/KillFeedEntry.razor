@using Sandbox;
@using Sandbox.UI;

@namespace Pace.UI
@attribute [StyleSheet]
@inherits Panel

<root>
	<label class="left" text=@Left></label>
	<img class="method" src=@Weapon.IconPath/>
	<label class="right" text=@Right></label>
</root>

@code
{
	public string Left { get; set; }
	public WeaponDefinition Weapon { get; set; }
	public string Right { get; set; }
	private RealTimeSince _timeSinceCreated = 0;

	public KillFeedEntry(string left, WeaponDefinition weapon, string right)
	{
		Left = left;
		Weapon = weapon;
		Right = right;
	}

	public override void Tick()
	{
		if (_timeSinceCreated > 6f)
			Delete();
	}
}